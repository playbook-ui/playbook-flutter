name: component_catalog_app
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  component:
    path: "../component"
  playbook_ui:
  playbook:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.0
  playbook_snapshot:
  playbook_generator:

flutter:
  uses-material-design: true
  fonts:
    - family: Roboto
      fonts:
        - asset: fonts/Roboto-Regular.ttf

dependency_overrides:
  playbook:
    path: ../../../playbook
  playbook_ui:
    path: ../../../playbook_ui
  playbook_snapshot:
    path: ../../../playbook_snapshot
  playbook_generator:
    path: ../../../playbook_generator
